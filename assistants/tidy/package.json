{
  "name": "@sketch-hq/sketch-tidy-assistant",
  "version": "4.0.1-next.8",
  "license": "MIT",
  "main": "dist/index",
  "types": "dist/index",
  "sketch": "dist/sketch.js",
  "repository": "github:sketch-hq/sketch-assistants",
  "homepage": "https://github.com/sketch-hq/sketch-assistants/tree/master/assistants/tidy",
  "sketch-assistant": {
    "title": "Tidy",
    "description": "The rules in this Assistant are all focused on keeping your Sketch documents as clean and tidy as possible",
    "icon": ""
  },
  "keywords": [
    "sketch",
    "sketch files",
    "sketch assistant",
    "assistant",
    "design ops"
  ],
  "files": ["dist"],
  "dependencies": {
    "@sketch-hq/sketch-assistant-types": "4.0.1-next.8"
  },
  "devDependencies": {},
  "scripts": {
    "test": "jest",
    "build": "tsc --build && yarn build:sketch",
    "build:sketch": "webpack --output-library _sketch --output-library assistants --output-library `node -p \"require('./package.json').name\"`",
    "package-tarball": "yarn build && yarn pack"
  }
}
